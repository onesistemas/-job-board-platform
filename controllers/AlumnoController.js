const AlumnoController = {
    actualizarPerfil: (req, res) => {
        // Lógica para actualizar perfil de alumno
        res.send("Perfil de alumno actualizado");
    },
    aplicarAnuncio: (req, res) => {
        // Lógica para aplicar a un anuncio
        res.send("Aplicación a anuncio realizada");
    }
};

module.exports = AlumnoController;
